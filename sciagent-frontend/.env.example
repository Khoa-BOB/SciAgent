# Base URL of sciagent-backend's KG service (kg_service).
KG_SERVICE_URL=http://localhost:8000

# One of the keys in sciagent-backend's KG_SERVICE_ALLOWED_KEYS.
# Server-side only -- never exposed to the browser (see lib/api.ts).
KG_SERVICE_KEY=
